# --- 1. PyTorch 官方源 (确保下载 GPU 版本) ---
--extra-index-url https://download.pytorch.org/whl/cu118

# --- 2. 核心底层依赖 (最优先安装) ---
# 关键：必须锁定 Numpy 1.x 版本，否则 PyTorch 2.0 会报错
numpy==1.26.4

# PyTorch 核心 (会自动从上面的 index-url 下载)
torch==2.0.0
torchvision==0.15.0

# --- 3. Web 框架 ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart

# --- 4. 图像处理 ---
# 自动选择适配 Python 3.10 的最新 4.x 版本
opencv-python>=4.9.0
pillow>=10.0.0
matplotlib>=3.8.0

# --- 5. AI 模型 (YOLO 等) ---
# 自动适配 PyTorch 2.0
ultralytics>=8.1.0
ultralytics-thop

# --- 6. 数据处理 ---
pandas>=2.2.0
scipy>=1.10.0

# --- 7. 系统与辅助工具 ---
psutil
py-cpuinfo
aiofiles
annotated-types
anyio
click
colorama
filelock
fsspec
h11
httpcore
httpx
pydantic>=2.5.0
pydantic_core
requests
starlette
typing_extensions
websockets
watchfiles

